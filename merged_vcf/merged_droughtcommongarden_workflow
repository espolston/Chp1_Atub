#Julia created merged files after including variant and invariant sites and normalizing
#unprocessed SNP vcf: /project/kreiner/pairedenv_commongarden/normalized_SNPsonly_vcfs/drought_commongarden_merged_normalized_filtsnps_nomiss30.vcf.gz
#unprocessed all sites vcf, for pixy: /project/kreiner/pairedenv_commongarden/allsites/droughtcgmerged_allsites_filtered.vcf.gz

#Will be using the same cutoffs for missingness and MAF in the SNP and all sites file

#.05 missingness and MAF .01 filters (for variant file), remove 2 indivs (P16_Nat_1_T and P12_Nat_14_T), rename all drought P12 to P10, add ID
#also checks for duplicate sites, creates binary, fam files and pca/mds and cmh scan
vcf_processing.sbatch
#downloaded dcgm_pca.eigenval, dcgm_pca.eigenvec, dcgm_mds.mds and dcgm_cmh.cmh from this and processed in chp1_mergedvcf.Rmd

#From here use: 
#Final SNP: /project/kreiner/pairedenv_commongarden/normalized_SNPsonly_vcfs/drought_commongarden_merged_finalprocessed_ID.vcf.gz
#Final all sites: /project/kreiner/pairedenv_commongarden/allsites/droughtcgmerged_allsites_finalprocessed_ID.vcf.gz

#Check relatedness
IBD.sbatch
#dcgm_IBD.genome.gz downloaded and analyzed in chp1_mergedvcf.Rmd
#Removing 27 individuals based on rules: 1. remove both indivs in .95 pi_hat pair 2. remove all individuals that have more than one contrast with pi_hat > .2 (so 4828 was in a pair with 5102 and P17_Ag_21 so I removed 4828) 3. Then of the remaining, the contrasts that were from different pairs, I removed the one from the larger sample size population 4. For the last 10, I randomly chose which one to remove (with a rand num generator)

#removes related individuals from snp and allsites vcf, runs plink and cmh scan
afterIBD.sbatch

#Moved extra vcf files into cds3 just in case and checked integrity of files in project
mv /project/kreiner/pairedenv_commongarden/normalized_SNPsonly_vcfs/drought_commongarden_merged_normalized_filtsnps_nomiss30.vcf.gz  /cds3/kreiner/dcgm_filteredvcf/ & mv /project/kreiner/pairedenv_commongarden/normalized_SNPsonly_vcfs/drought_commongarden_merged_normalized_filtsnps_nomiss30_rehead.vcf.gz /cds3/kreiner/dcgm_filteredvcf/
module load htslib
bgzip -t /project/kreiner/pairedenv_commongarden/normalized_SNPsonly_vcfs/drought_commongarden_merged_final_ID_IBD.vcf.gz
bgzip -t /project/kreiner/pairedenv_commongarden/allsites/droughtcgmerged_allsites_final_ID_IBD.vcf.gz

#Whatshap as input to shapeit4 (phasing)
#Made list of bams needed and new sample names in chp1_mergedvcf.Rmd
#reheads vcf, runs whatshap
sh copy_whatshapbams.sh
sbatch whatshap.sbatch

#chp1_mergedvcf.Rmd has all off server work (MDS/PCA, IBD, phase prep, cmh)
